import React from 'react';
import { NavBar } from 'antd-mobile';
import styles from './header-search.less';

function Headertitle({title = "" , goBack}) {

  return(
      <NavBar className={styles["navbar-fixed"]}
        mode="light"
        leftContent="返回"
        onLeftClick={goBack}
      >{title}</NavBar>
  )
}
export default Headertitle
